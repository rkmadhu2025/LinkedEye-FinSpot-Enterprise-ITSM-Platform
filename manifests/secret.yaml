apiVersion: v1
kind: Secret
metadata:
  name: n8n-api-keys
  namespace: n8n
# Secrets must be created outside of the repository. Use a secret manager or:
# kubectl create secret generic n8n-api-keys --from-literal=OPENAI_API_KEY='<REPLACE_ME>'
type: Opaque
stringData: {}
